# switch 用法

```c
switch (expression) {
    case value1:
        // 执行代码块1
        break;
    case value2:
        // 执行代码块2
        break;
    // 可以有更多的 case
    default:
        // 如果没有匹配的 case，执行默认的代码块
}
```

