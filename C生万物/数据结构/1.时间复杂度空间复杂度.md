# 时间复杂度

**时间复杂度**：时间复杂度是衡量算法执行时间随着输入规模增长而增长的速度。通常使用大 `O` 记法表示

一般情况下：**O(1) < O($log_2$ n) < O(n) < O($n^2$) < O($n^3$) < O($2^n$) < O(n!)**



# 空间复杂度

**空间复杂度**：空间复杂度是算法在执行过程中所需的内存空间，也使用大 `O` 记法表示

一般情况下：**O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ) < O(n!)**
