# 动态规划

动态规划的基础是**空间换时间**

1.  **:one: 状态定义**

   明确问题的「状态」—— `dp[i]` 或 `dp[i][j]` 代表什么

   斐波那契：`dp[i]` 表示第 i 个斐波那契数

2.  **:two: 状态转移方程**

   明确「状态转移方程」—— `dp` 如何从过去推到现在

   斐波那契：`dp[i] = dp[i-1] + dp[i-2]`

3.  **:three: 初始状态**

   确定「初始值」—— `dp` 的 `base case`

   斐波那契：`dp[0] = 0, dp[1] = 1`

4.  **:four: 遍历顺序**

   确定「遍历顺序」—— 一维/二维、正序/倒序

   不同 `DP` 遍历顺序不同：

5.  **:five: 返回答案**

   返回答案（通常是 `dp[n]` 或 `dp[n-1][m-1]` 或 `max(dp)`）

   斐波那契：`return dp[n]`